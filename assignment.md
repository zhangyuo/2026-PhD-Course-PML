课程工具顶层框架
课程覆盖 4 个核心工具/概念，从简到难：


Block 1: 概率回归 → 建模"变量之间的不确定关系"
Block 2: 概率分类 → 建模"预测置信度/类别概率"
Block 3: 概率聚类 → 建模"无标签数据的隐藏结构"
Block 4: VAE      → 建模"数据的压缩隐变量表示"
贯穿全程: SVI (变分推断) → 不能精确求后验时的近似方法
每个工具的核心思想一句话：

工具	核心思想	输出什么
概率回归	参数不是点估计，是分布	预测值的不确定区间
概率分类	不只输出类别，输出置信度分布	每个类别的后验概率
GMM-高斯混合模型聚类	数据来自K个高斯的混合	软聚类分配 + 每个簇的参数
VAE	高维数据 → 低维隐变量 → 重建	隐变量 z 的后验分布
SVI	用优化代替积分求后验	近似后验 q(θ) ≈ p(θ|data)
三个分析维度 × 课程工具 的对应

维度1: 特征层 (x_orig → x_wm)
         ↕  Δx 是什么分布？水印节点是否偏离正常特征分布？
         → 工具: VAE (学习特征空间的正常分布，检测离群)
                GMM (聚类特征空间，水印节点落入哪个簇)

维度2: 嵌入层 (H_benign → H_mark)
         ↕  embedding 偏移量 ΔH 是有方向的吗？偏移多少？
         → 工具: 概率回归 (建模 ΔH 与 节点属性的关系)
                GMM (H_benign vs H_mark 的聚类结构变化)

维度3: 预测层 (softmax → 后验预测分布)
         ↕  水印节点预测是否更不确定？模型对自己"不确定"的节点是哪些？
         → 工具: 概率分类 + SVI (给每个预测配置信度区间)
具体来说：

维度1 → VAE 是最合适的

训练一个 VAE 在 x_orig 特征上，学到正常论文特征的隐变量分布 p(z)。然后把 x_wm 通过 encoder，看它的重建误差和 KL 散度——水印节点应该有更高的重建误差（偏离正常分布），这直接量化了水印的"隐蔽性代价"。

维度2 → GMM + 概率回归

对 H_benign 做 GMM 聚类（40类，每类一个高斯），再把 H_mark 代入，看水印节点是否被分配到不同的软聚类，软分配变化 = embedding 偏移的结构性影响。另外可以用概率回归建模：ΔH_v 能否被节点度数 d_v 解释？（你的理论：低度节点响应更大）

维度3 → 贝叶斯分类 + SVI

在 H_benign 上训练贝叶斯逻辑回归（Pyro SVI），权重有后验分布。然后用水印节点的 H_mark 做预测，输出是后验预测分布。预测熵 = 不确定性，水印节点应该熵更高。

作业叙事主线建议

"水印注入从三个层次影响图模型：
 特征层（隐蔽性），嵌入层（有效性），预测层（不确定性）。
 我们用三种概率工具分别量化这三种影响。"
这个叙事结构天然对应6章节：

Chapter 1: 问题背景 + ogbn-arxiv 数据集
Chapter 2: EDA（特征/度分布/类别分析）
Chapter 3: 三个概率模型设计（VAE + GMM + Bayesian分类）
Chapter 4: 三个模型的 SVI 推断过程
Chapter 5: 三层分析结果（主要贡献）
Chapter 6: 综合讨论：水印的隐蔽性-有效性权衡